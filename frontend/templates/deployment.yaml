apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: expense
  labels:
    app: frontend
    tier: web
    project: expense
spec:
  replicas: {{ .values.deployement.replicas }}
  selector:
    matchLabels:
      app: frontend
      tier: web
      project: expense
  template:
    metadata:
      labels:
        app: frontend
        tier: web
        project: expense
    spec:
      containers:
      - name: frontend
        image: chaitanyaj293/frontend:{{ .values.deployement.imageversion }}
        resources:
          requests:
            cpu: {{ .values.deployment.cpu }}
            memory: {{ .values.deployment.memory }}
          limits:
            cpu: {{ .values.deployment.cpu }}
            memory: {{ .values.deployment.memory }}
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: expense
spec:
  type: LoadBalancer
  selector:
    app: frontend
    tier: web
    project: expense
  ports:
  - name: frontend-svc-port
    protocol: TCP
    port: {{ .values.service.port }}
    targetPort: {{ .values.service.targetPort }}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
 name: frontend
 namespace: expense
spec:
 scaleTargetRef:
   apiVersion: apps/v1
   kind: Deployment
   name: frontend
 minReplicas: 1
 maxReplicas: 10
 targetCPUUtilizationPercentage: {{ .values.hpa.targetCPUUtilizationPercentage }} # usually 75 in real environment
